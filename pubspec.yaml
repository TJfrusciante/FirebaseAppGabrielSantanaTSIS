name: myapp
description: "Projeto flutter da disciplina desenvolvimento de sistemas 2 - Gabriel Santana"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  cupertino_icons: ^1.0.8
  firebase_auth: ^6.2.0
  firebase_core: ^4.5.0
  provider: ^6.1.5+1
  cloud_firestore: ^6.2.0
  flutter_local_notifications: ^20.1.0
  permission_handler: ^12.0.1
  latlong2: ^0.9.1
  flutter_map: ^8.3.0
  http: ^1.6.0
  intl: any

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  generate: true
  uses-material-design: true